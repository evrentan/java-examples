# Read file on the Config Server
# .properties or .yml file on config server
# local, dev
spring.application.name = java-record-example

# Config server setting
spring.cloud.config.uri = http://localhost:8888